import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

const state = {
}

const mutations = {
    // mutationsMethos (state, value) {
    //     state.xxx = value
    // }
}

export default new Vuex.Store({
    state, mutations
})
